# Changelog

## 0.0.1

Initial release.

- Installed package list from `.csproj` / `.fsproj` / `.vbproj`
- TFM-aware version filtering against the NuGet v3 API
- Update level (patch / minor / major), prerelease toggle, transitive view
- Webview UI in the panel area
